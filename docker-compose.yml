version: "3.9"

services:
  dspace-light-ui:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: /server
    ports:
      - "5173:8080"
    restart: unless-stopped
